/**
 * Firebase Cloud Functions entry — DISABLED.
 * Production backend is Vercel (`api/index.js` + `server/`).
 * Deploying this file would require Blaze; do not use it.
 */
throw new Error(
  'Firebase Cloud Functions are disabled for Noah Pets. Use Vercel Serverless (api/ + server/).',
)
